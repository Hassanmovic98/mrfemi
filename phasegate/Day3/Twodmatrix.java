import java.util.Array;
public class Twodmatrix {
	public static void main (String[] args) {
	
	int number = 0;
	int realnumbers = 0;

	int [] [] number = new int [numbers.length] [numbers.length];
	
	
 for (int index=nums.length-1 index >= 0; index--) {
	for (int count = 0; count < number[index]; count++) {
		new array [] []= numbers[][]
	 counter +=1
	for (num = 0; num < num.length; num++)

	realnumbers = (index)(value) = numbers(index)(value);


















     for(int row = 0; rows < numbers.length ; rows++) {
	(arrayvalue[rows] = array [value][row]-1;
	 for(int col=0; col < numbers.length ; column++) {
 